int RandomArray(int[] array) // заполение массива положительными трехзначными числами
{
    for (int i = 0; i < array.Length; i++)
    {
        array[i] = new Random().Next(100, 1000);
    }
    return array;
}
int PositiveNumbers(int[] array)
{
    for (int i = 0; i < array.Length; i++)
    {
        if array[i]
    }
}

int[] array = new(5);
RandomArray(array);
